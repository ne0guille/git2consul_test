config:
    application:
        myKey: "appValue"
    example-pubsub:
        myKey: "example-value"
levelsbeyond:
    base-url: "recon-dc1.local"
    routes:
        example-pubsub: "/v1/messages /v1/foo/bar /v1/test/guille"

